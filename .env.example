# Required for the default OpenRouter adapter (Player/Coach/Judge models)
OPENROUTER_API_KEY=sk-or-v1-...

# Required for the 15-problem benchmark example (Claude Sonnet baseline only)
# Optional — only the benchmark uses Anthropic; the dialectic itself does not.
ANTHROPIC_API_KEY=sk-ant-...
